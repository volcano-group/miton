{
  "name": "static-site-example",
  "private": true,
  "scripts": {
    "build": "node index.js && workbox generateSW workbox-config.js",
    "sw": "workbox generateSW workbox-config.js",
    "serve": "http-server ./build/ -p 8081 -S -C cert.pem -K key.pem -a localhost"
  },
  "dependencies": {
    "handlebars": "^4.0.5",
    "http-server": "^0.11.1",
    "metalsmith": "^2.1.0",
    "metalsmith-discover-partials": "^0.1.0",
    "metalsmith-handlebars": "^1.1.0",
    "metalsmith-layouts": "^1.4.1",
    "metalsmith-markdown": "^0.2.1",
    "metalsmith-partial": "^0.1.0",
    "metalsmith-permalinks": "^0.5.0",
    "workbox": "^0.0.0"
  }
}
